{
  "name": "starship-forge",
  "private": true,
  "version": "0.1.0",
  "description": "星舰熔炉 — 核心规则无头测试（core/ 零 cc 依赖，直接在 node 中回归）",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
